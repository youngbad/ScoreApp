hometab_ui <- tabItem(tabName = 'home_tab',
                      fluidRow(align = 'center',
                               wellPanel(
                                 fluidRow(align="center",
                                          div("ScoreApp", style = 'font-size: 160%'))
                                         )
                               )
                      )